require 'Game'

describe Code, "given" do

    xit "the game input '1, 2, 5, 6' to take_input, it outputs an error message and redirects user by calling method again" do
      #expect(Game.take_input([1, 2, 5, 3]).convert).to_eq #something
    end
end
